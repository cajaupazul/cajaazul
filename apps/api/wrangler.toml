name = "campuslink-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Variables de entorno no secretas (públicas)
[vars]
ALLOWED_ORIGIN = "https://cajaupazul.pages.dev"

# Los secretos se configuran vía command line (wrangler secret put)
# - SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
# - MP_ACCESS_TOKEN
